
// Woohoo, let's get racing!
// Loads pages before you can start

document.addEventListener('DOMContentLoaded', function() {

// Let's get our dance trance cats party started!

var active = document.getElementByClass("active");

var cat1move = document.getElementById.innerHTML("cat1move");
var cat2move = document.getElementById.innerHTML("cat2move");

var trackRow = document.querySelectorAll("tr");
var trackDistance = document.querySelectorAll("td");

var td = document.querySelector("td");
var tr = document.querySelector("tr");



// Make the race track for each player

for (var i = 0; i < allTd.length; i++){
    
};

// Move forward function done in 2 parts - removing and adding 

var moveForward = function(){
  if(td[i] === ("active")){
    document.getElementByClass("active").appendChild(document.getElementByClass(""));

  } 

  else if(td[i] === ("")){

  }

// Add some key presses - A key = 65, L key = 76

function keyPress(event){
  if (event.which === 65) {
      cat1move.innerHTML = 
  } else {
      if (event.which === 76){
        cat2move.innerHTML = 
      }
  };

// Finish line! #Winning 




// Player wins modal



};